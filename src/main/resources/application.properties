spring.application.name=event-ticket


# Database Connection
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=changemeinprod!


# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:9090/realms/event-ticket-platform

#JPA, or Java Persistence API, is a Java specification that defines a standard for managing \
#  persistence and object-relational mapping (ORM) in Java applications. It provides a \
#  standardized way to interact with relational databases using Java objects, bridging the gap between the \
#  object-oriented programming paradigm and relational database systems.

# JPA automatically manages database schema changes